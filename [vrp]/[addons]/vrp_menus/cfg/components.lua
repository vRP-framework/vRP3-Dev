-- Loaded client-side and server-side.
--
-- Enable/disable menu components (some may be required by others).
-- It's recommended to disable things from the componts configurations directly if possible.

local menus = {
	base = true,
	admin = true,
  group = true,
  weather = true,
  banking = true,
  weapon = true,
  identity = true,
  vehicle = true
}

return menus
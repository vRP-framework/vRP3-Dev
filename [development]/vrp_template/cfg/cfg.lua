local cfg = {}

-- add your resource's config here -- read on both sides via
-- module("vrp_template", "cfg/cfg") (swap "vrp_template" for your
-- resource's actual name if you copy this template elsewhere)

return cfg
